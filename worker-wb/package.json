{
  "name": "ali-ai-worker-wb",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker deployment root for ALI-ai chassis runtime",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.83.0"
  }
}
