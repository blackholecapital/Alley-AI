CLOUDFLARE ACCESS MATRIX — EXEC-AI-STAGE3-004 | CF-PREP | WORKER B
===================================================================
Generated: 2026-04-17
Branch:    claude/verify-cloudflare-deploy-config-ZViq8
Stage:     CF-PREP (Cloudflare Deployment Prep)
Task:      Verify Access expectations for /, /health,
           /telegram/webhook, /session/latest, and /calendar/status
           and record any path likely to be blocked or mismapped.

-------------------------------------------------------------------
1. SOURCE OF TRUTH
-------------------------------------------------------------------
build sheet:        /job_site/build-sheet-EXEC-AI-STAGE3-004.txt
wrangler config:    /worker-wb/wrangler.jsonc
worker entrypoint:  /worker-wb/src/index.ts
static assets root: /worker-wb/public   (wrangler "assets.directory": "./public")
prior evidence:     /job_site/cloudflare_prep_report.txt (Stage 3, this pass)
                    Stage 2 CF-PREP artifacts (EXEC-AI-STAGE2-003)

build-sheet declarations (Stage 3):
  access_policy_expected:      bypass_paths
  required_public_test_routes: / , /health , /telegram/webhook ,
                               /session/latest , /calendar/status
  workers_dev_required:        yes
  preview_urls_required:       yes
  custom_domain_if_any:        existing Stage 2 live custom domain

wrangler.jsonc declarations:
  name:          "ali-ai"
  main:          "src/index.ts"
  workers_dev:   true     (non-gated *.workers.dev URL available)
  preview_urls:  true
  assets:        { directory: "./public" }   (static UI shell)

-------------------------------------------------------------------
2. ACCESS SURFACE MODEL
-------------------------------------------------------------------
Two public surfaces exist for the same Worker ("ali-ai"):

  A. workers.dev surface — ali-ai.<account>.workers.dev
     - Not covered by Cloudflare Access / Zero Trust.
     - Reachable by curl, Telegram, browsers without login.
     - Declared "public testing" surface for CF-TEST evidence.
     - Confirmed by wrangler.jsonc workers_dev: true.

  B. Custom-domain surface — the Stage 2 live custom domain
     - Fronted by Cloudflare Access (Zero Trust) from Stage 1/2.
     - Default behavior: 302 redirect to Access login on every path.
     - Only paths in an Access "Bypass" policy are reachable by
       anonymous clients (browsers, curl, Telegram, Google push).
     - Stage 1/2 operator notes flag "Access overlap" as the primary
       prior time-loss pattern; this matrix pre-empts it for Stage 3.

-------------------------------------------------------------------
3. PATH-BY-PATH ACCESS EXPECTATIONS
-------------------------------------------------------------------

PATH:              /
  Served by:       /worker-wb/public/index.html (static asset handler)
  Consumer:        human operator browser (UI shell)
  workers.dev:     PUBLIC — no Access.            Expected: 200 HTML
  Custom domain:   MUST be Access-bypassed if the demo expects an
                   anonymous UI load. If UI is behind Access, the
                   operator must log in before every demo run.
                   Expected after bypass: 200 HTML.
  Current worker:  Asset handler serves public/index.html; src/index.ts
                   returns 404 only for unmatched non-asset paths.
  Likely issue:    If Access is zone-wide with no "/" bypass, the UI
                   will 302 to the Access login page instead of the
                   shell. Stage 3 demo path assumes anonymous load.

PATH:              /health
  Served by:       /worker-wb/src/routes/health.ts (handleHealth)
  Consumer:        operator probe, CF-TEST checklist, uptime curl
  workers.dev:     PUBLIC — no Access.            Expected: 200 JSON {ok:true}
  Custom domain:   MUST be in the Access Bypass path set
                   (access_policy_expected = bypass_paths).
                   Expected after bypass: 200 JSON.
  Current worker:  WIRED. Handler present in src/index.ts route table.
  Likely issue:    If bypass is missing on the custom domain, probes
                   return Access login HTML (302) instead of JSON;
                   CF-TEST healthcheck evidence will fail even though
                   the Worker itself is healthy.

PATH:              /telegram/webhook
  Served by:       /worker-wb/src/routes/telegram-webhook.ts
                   (handleTelegramWebhook)
  Consumer:        Telegram servers (third-party). Telegram cannot
                   authenticate through Cloudflare Access — no
                   interactive login, no service-token headers.
  workers.dev:     PUBLIC — no Access.            Expected: 200 ack
                   THIS IS THE ONLY SAFE WEBHOOK SURFACE unless a
                   bypass rule is confirmed on the custom domain.
  Custom domain:   MUST be Access-bypassed. Any Access gate will
                   drop 100% of Telegram updates with a 302 to the
                   login page; Telegram marks them as last_error.
  Current worker:  WIRED. Handler present in src/index.ts route table.
  Likely issue:    HIGH-RISK BLOCK on custom domain if the webhook
                   URL ever points there without a bypass policy.
                   CF-Access-Client-Id / Secret cannot be used —
                   Telegram does not send those headers. Keep webhook
                   registered against the *.workers.dev URL for the
                   Stage 3 demo unless operator confirms the bypass.

PATH:              /session/latest
  Served by:       /worker-wb/src/routes/session-latest.ts
                   (handleSessionLatest)
  Consumer:        operator UI shell (same-origin XHR from "/"),
                   visible-event render loop.
  workers.dev:     PUBLIC — no Access.            Expected: 200 JSON state
  Custom domain:   MUST be Access-bypassed whenever the UI itself is
                   anonymous. If "/" is bypassed but /session/latest
                   is not, XHR receives Access login HTML instead of
                   JSON and the UI goes blank. Both paths must share
                   the same Access posture (both bypassed or both
                   gated).
  Current worker:  WIRED. Handler present in src/index.ts route table.
                   UI (/worker-wb/public/js/app.js) now fetches
                   /session/latest directly — Stage 2 legacy
                   /api/status + /api/message drift is resolved.
  Likely issue:    Only Access-posture risk remains; no route-contract
                   drift. If bypass is split between "/" and
                   /session/latest on the custom domain, the UI will
                   render empty with 302 HTML in the XHR response.

PATH:              /calendar/status
  Served by:       /worker-wb/src/routes/calendar-status.ts — NOT YET
                   IMPLEMENTED (to be delivered by Stage S4, "Single
                   calendar action").
  Consumer:        operator UI shell (same-origin XHR) and operator
                   CF-TEST curl probe for the Stage 3 calendar path.
  workers.dev:     PUBLIC — no Access.            Expected: 200 JSON state
  Custom domain:   MUST be Access-bypassed. Sits in the same public
                   probe set as /health and /session/latest.
  Current worker:  MISSING. src/index.ts has no '/calendar/status'
                   branch today — falls through to 404. Grep for
                   "calendar" under /worker-wb/src returns no matches.
                   Declared S4 expected_artifacts confirm this:
                       /worker-wb/src/integrations/calendar/index.ts
                       /worker-wb/src/integrations/calendar/provider.ts
                       /worker-wb/src/routes/calendar-status.ts
                       /worker-wb/src/lib/action-router.ts
  Likely issue:    MISMAPPED (route not implemented) AND potentially
                   BLOCKED (bypass policy must be extended to include
                   /calendar/status before CF-TEST). Stage S4 owns the
                   handler; this matrix flags the Access-side work that
                   must accompany it so the path is reachable end-to-end.

-------------------------------------------------------------------
4. SUMMARY MATRIX
-------------------------------------------------------------------
path                 workers.dev   custom-domain     worker handler   status
-------------------  ------------  ----------------  ---------------  -----------
/                    public ok     bypass or gated   assets OK        OK-ish
/health              public ok     must-bypass       WIRED            OK on *.dev
/telegram/webhook    public ok     must-bypass       WIRED            OK on *.dev
/session/latest      public ok     must-bypass       WIRED            OK on *.dev
/calendar/status     public ok     must-bypass       MISSING (S4)     BLOCK until S4

Legend:
  OK-ish     = reachable via asset handler today; only Access posture
               remains to verify.
  OK on *.dev= returns the declared response on the *.workers.dev URL;
               custom-domain response depends on Access bypass policy.
  MISSING    = no handler in src/index.ts for this path yet.
  must-bypass= required in the Access "Bypass" path set per
               access_policy_expected = bypass_paths.
  BLOCK      = will not return the declared response even on *.dev
               because the route is not implemented.

-------------------------------------------------------------------
5. PATHS LIKELY TO BE BLOCKED OR MISMAPPED
-------------------------------------------------------------------
BLOCKED (Access-layer risk on custom domain):
  - /telegram/webhook   — third-party caller (Telegram) cannot clear
                          Access; highest-priority bypass. Keep webhook
                          pointed at the *.workers.dev URL until bypass
                          is confirmed.
  - /session/latest     — browser XHR from UI fails if Access redirects
                          to login HTML instead of returning JSON.
  - /calendar/status    — operator probe AND UI XHR; must share the
                          bypass posture of /session/latest and /health.
  - /health             — operator/CI probes fail against the custom
                          domain without a bypass rule.

MISMAPPED (route not implemented or contract drift):
  - /calendar/status    — declared Stage 3 required public route; no
                          handler in /worker-wb/src/index.ts; handler
                          file /worker-wb/src/routes/calendar-status.ts
                          does not exist yet. Stage S4 is the single
                          owner and must deliver:
                              src/integrations/calendar/index.ts
                              src/integrations/calendar/provider.ts
                              src/routes/calendar-status.ts
                              src/lib/action-router.ts
                          BEFORE CF-TEST, or CF-TEST will fail.
  - /                   — static asset handler serves public/index.html
                          today; verify no route pattern added in S2–S5
                          shadows the asset at "/".
  - /health, /telegram/webhook, /session/latest — NO route-contract
                          drift observed this stage; all three resolve
                          to declared handlers in src/index.ts. Stage 2
                          UI drift to /api/status and /api/message is
                          resolved; app.js now targets /session/latest.

-------------------------------------------------------------------
6. RECOMMENDED ACCESS POSTURE (for CF-PREP handoff)
-------------------------------------------------------------------
- Keep workers_dev = true as the guaranteed-public test surface for
  CF-TEST evidence and for Telegram webhook registration until a
  bypass policy is confirmed on the custom domain.
- On the Stage 2 live custom domain, the Access Application must
  include a Bypass policy for exactly these five paths (one more than
  Stage 2):
      /
      /health
      /telegram/webhook
      /session/latest
      /calendar/status
- Do NOT rely on a service-token (CF-Access-Client-Id /
  CF-Access-Client-Secret) header for /telegram/webhook — Telegram
  will not send those headers.
- Before Stage S2 text-path work and Stage S4 calendar-route work,
  confirm with operator which surface the Stage 3 demo runs against
  (workers.dev vs custom domain):
      - If workers.dev: no Access change needed; webhook URL and UI
        origin must both use the *.workers.dev hostname.
      - If custom domain: the five-path bypass above is a hard
        prerequisite before CF-TEST.
- Stage S4 delivery checklist should explicitly include updating the
  custom-domain Access bypass list to add /calendar/status at the
  same time the route handler is deployed, so posture and code land
  together.

-------------------------------------------------------------------
7. ARTIFACT SELF-CHECK
-------------------------------------------------------------------
expected_artifact:          /job_site/cloudflare_access_matrix.txt
written_at:                 /home/user/ALI-ai/job_site/cloudflare_access_matrix.txt
covers five declared paths: yes
                              /
                              /health
                              /telegram/webhook
                              /session/latest
                              /calendar/status
records blocks/mismaps:     yes (section 5)
required_references_read:
  - /job_site/factory-manual-v1.1.txt
      (present on disk as /job_site/factory-manual-v1.11.txt — same
       active manual per PRECEDENCE LAW; name-version drift flagged
       only, no content conflict)
  - /job_site/worker-execution-manual-v1.1.txt      (present, matches)
  - /job_site/build-sheet-EXEC-AI-STAGE3-004.txt    (present, matches)

branch identity:            claude/verify-cloudflare-deploy-config-ZViq8
repo_mirror_required:       no  (per build sheet worker_b CF-PREP)
commit_required:            no  (per build sheet worker_b CF-PREP)
push_required:              no  (per build sheet worker_b CF-PREP)
