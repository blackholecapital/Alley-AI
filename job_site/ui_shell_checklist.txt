UI SHELL CHECKLIST
job_id:   EXEC-AI-STAGE4-UIFIX-002
worker:   WORKER B
stage:    S2
artifact: /job_site/ui_shell_checklist.txt
source:   /apps/operator-shell/src/ui/index.html
          /apps/operator-shell/src/ui/session-view.js
          /apps/operator-shell/src/ui/voice-capture.js
          /apps/operator-shell/src/ui/calendar-panel.js

PURPOSE
Verify the deployed interface contains only the intended controls for this run
and identify any missing or broken surfaces before S2 asset-alignment work begins.

RESULT CODES
  PRESENT — control exists in the HTML and is wired to JS
  MISSING — control does not exist in the HTML (gap to be resolved in S2/S3/S4)
  PARTIAL — control has JS logic but no HTML surface
  PASS    — fully present, wired, and meets the contract spec

────────────────────────────────────────────────────────────────────────────────
CONTROL 1 — CHAT INPUT
────────────────────────────────────────────────────────────────────────────────
Expected:   A visible text input (textarea or input[type=text]) allowing the
            operator to compose and submit a browser-originated text message.
DOM hook:   No element found — no <textarea>, no <input type="text">,
            no #chat-input, no #send-btn in index.html.
JS status:  sendMessage() defined in session-view.js (line 336); POSTs to
            /telegram/webhook. Function is exported but NOT wired to any
            HTML element.
Route:      POST /telegram/webhook (declared, exists in worker-wb)
Result:     MISSING
Gap note:   Chat input and send button must be added to index.html and wired
            to sendMessage() before the browser chat route is testable.

────────────────────────────────────────────────────────────────────────────────
CONTROL 2 — SEND BUTTON
────────────────────────────────────────────────────────────────────────────────
Expected:   A button that submits the chat input text to the assistant.
DOM hook:   No element found in index.html.
JS status:  sendMessage() available but no click handler wired to any button.
Result:     MISSING
Gap note:   Must be added alongside chat input (Control 1 above).

────────────────────────────────────────────────────────────────────────────────
CONTROL 3 — TALK BUTTON (voice start)
────────────────────────────────────────────────────────────────────────────────
Expected:   A button that requests mic permission and begins recording.
DOM hook:   #voice-btn-start  (index.html line 515)
            .voice-btn-start  styled as primary accent action
            aria-label="Start recording"
JS status:  Wired to startCapture() from voice-capture.js (index.html line 633)
            startCapture() calls navigator.mediaDevices.getUserMedia({audio:true})
            Transitions: idle → requesting → recording
State badge:#voice-status-badge reflects current state with aria-live
Result:     PRESENT
Notes:      Talk label collapses to mic glyph only at ≤420px viewport.

────────────────────────────────────────────────────────────────────────────────
CONTROL 4 — STOP & SEND / RECORDING CONTROLS
────────────────────────────────────────────────────────────────────────────────
Expected:   Controls to end recording and submit audio, or cancel.
DOM hooks:  #voice-btn-stop    "Stop & Send"  (index.html line 521)
            #voice-btn-cancel  "Cancel"       (index.html line 527)
JS status:
  Stop & Send: wired to stopAndSend() (voice-capture.js line 115)
               Transitions: recording → processing → sent → idle (3 s)
               POSTs to /voice/capture (multipart/form-data, field "audio")
  Cancel:      wired to cancelCapture() (voice-capture.js line 136)
               Returns to idle without sending
Visibility:  Stop & Send: hidden when not recording; shown only during recording
             Cancel:      hidden when idle or busy; shown when recording or terminal
Result:     PRESENT
Notes:      Both buttons disabled unless in the correct state; aria-label set.

────────────────────────────────────────────────────────────────────────────────
CONTROL 5 — CALENDAR ENTRY
────────────────────────────────────────────────────────────────────────────────
Expected:   A visible calendar surface showing provider state and one testable
            action path from the browser.
DOM hooks:
  Status bar:
    #status-calendar           — live pill in status bar showing provider readiness
    #calendar-panel-toggle     — "⚙ Calendar" button that opens/closes drawer
  Drawer (#calendar-panel):
    #calendar-panel-provider   — provider name
    #calendar-panel-state      — configured/unconfigured/ready/not_ready pill
    #calendar-panel-reason     — not-ready reason (hidden when ready)
    #calendar-panel-detail     — detail text (hidden when ready)
    #calendar-panel-guidance   — guidance block for demo/setup paths
    #calendar-panel-action-type — action type label (calendar.list_today)
    #calendar-panel-run        — trigger button for calendar.list_today
    #calendar-panel-result     — result surface (ok/fail, reply_text, meta)
    #calendar-panel-error      — network/route error line
  Action bar:
    #calendar-action-bar       — strip shown after invocation
    #calendar-action-result    — last action result text
JS status:
  calendar-panel.js wires toggle, close, and run button
  refreshStatus()  — GET /calendar/status on open and auto on init
  triggerAction()  — GET /calendar/status?run=1 on run button click
  classifyStatus() — maps provider response to one of 6 closed UI states
  Six closed states: unknown, ready, not_ready, unconfigured, unsupported, error
Result:     PRESENT
Notes:      Calendar drawer is full-width (100vw) at ≤640px viewport.
            Run button disabled only while state is "unknown" — always runnable
            in configured/unconfigured states so both paths are operator-testable.

────────────────────────────────────────────────────────────────────────────────
CONTROL 6 — STATUS STRIP
────────────────────────────────────────────────────────────────────────────────
Expected:   A persistent strip showing core assistant state, session ID, and
            calendar readiness.
DOM hooks:  .status-bar           (index.html line 404) — sticky top header
            #status-core          — live / loading / idle / error
            #status-state         — current UIRenderState.kind
            #status-session       — session_id (first 16 chars, monospace)
            #status-calendar      — calendar provider readiness
            #last-event-marker    — "last event Xs ago" recency badge
JS status:  All five hooks driven by session-view.js; setState(), setText(),
            renderCalendar() update them on each poll (every 5 s).
Result:     PRESENT
Notes:      status-dividers and #last-event-marker hidden at ≤768px to reduce
            visual noise on narrow viewports.

────────────────────────────────────────────────────────────────────────────────
CONTROL 7 — MOBILE-SAFE LAYOUT
────────────────────────────────────────────────────────────────────────────────
Expected:   All controls accessible and readable on a mobile viewport without
            horizontal scroll or overlapping elements.
Implementation:
  Viewport meta:  width=device-width, initial-scale=1.0 (index.html line 5)
  ≤768px:         .panels switches from 2-column to 1-column 2-row grid
                  .status-dividers hidden; title font reduced to 12px
                  #last-event-marker hidden
                  .voice-footer padding and button size reduced
  ≤640px:         .calendar-panel width becomes 100vw (no left border)
  ≤420px:         .voice-btn-label hidden; buttons show icon-only
                  (Talk mic glyph &#127899;, Stop ■, Cancel ✕ remain visible)
  Panel sizing:   height: calc(100vh - header - footer) keeps panels in viewport
  Voice footer:   position: fixed; bottom: 0 — always accessible above fold
Result:     PRESENT

────────────────────────────────────────────────────────────────────────────────
SUMMARY
────────────────────────────────────────────────────────────────────────────────
Control                    Result
─────────────────────────  ───────
1  Chat input              MISSING
2  Send button             MISSING
3  Talk (voice start)      PRESENT
4  Stop & Send / Cancel    PRESENT
5  Calendar entry          PRESENT
6  Status strip            PRESENT
7  Mobile-safe layout      PRESENT

PASS count:    5 of 7
MISSING count: 2 of 7

BLOCKING GAPS FOR S3 (browser chat route):
  - Add <textarea id="chat-input"> or <input type="text" id="chat-input"> to
    index.html footer or chat section
  - Add <button id="send-btn"> wired to sendMessage() in session-view.js
  - sendMessage() already exists at session-view.js:336 and posts to
    /telegram/webhook — only the HTML surface is absent

NON-BLOCKING FOR THIS STAGE:
  Controls 3–7 are operator-testable today. Voice and calendar surfaces meet
  the S2 shell contract. Mobile layout is verified by CSS media query inspection.

NEXT: Foreman A to confirm asset alignment (S2 Worker A) then unblock S3 chat
      input addition.
