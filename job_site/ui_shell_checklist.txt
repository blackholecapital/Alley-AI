UI SHELL CHECKLIST
job_id:   EXEC-AI-STAGE4-UIFIX-002A
worker:   WORKER B
stage:    S2
artifact: /job_site/ui_shell_checklist.txt
sources:  /worker-wb/public/index.html
          /worker-wb/public/session-view.js
          /worker-wb/public/voice-capture.js
          /worker-wb/public/calendar-panel.js
references:
  /job_site/ui_shell_contract.txt
  /job_site/build-sheet-EXEC-AI-STAGE4-UIFIX-002A.txt
  /job_site/deploy_route_lock.txt

RESULT CODES
  PRESENT — control exists in HTML and is wired to the declared JS/route
  MISSING — control absent from HTML or wiring broken
  PASS    — present, wired, and meets full shell contract spec

────────────────────────────────────────────────────────────────────────────────
CHECK 0 — EXPLICIT ROUTE WIRING
────────────────────────────────────────────────────────────────────────────────
Verified client-side endpoint constants against declared route table:

  session-view.js:29   SEND_ENDPOINT     = '/ui/send'
    → declared handler: worker-wb/src/routes/ui-send.ts
    → constant matches route table: YES

  voice-capture.js:23  VOICE_ENDPOINT    = '/voice/capture'
    → declared handler: worker-wb/src/routes/voice-capture.ts
    → constant matches route table: YES

  session-view.js:28   CALENDAR_ENDPOINT = '/calendar/status'
    → declared handler: worker-wb/src/routes/calendar-status.ts
    → constant matches route table: YES

  No /telegram/webhook reference remains in client-side UI constants.
  Note: The prior contract (002) incorrectly listed /telegram/webhook as the
  chat endpoint. The actual constant in session-view.js is /ui/send. This
  checklist reflects the actual codebase.

Result:   PRESENT
Caveat:   Handler files (ui-send.ts, voice-capture.ts, calendar-status.ts) must
          be confirmed present in worker-wb/src/routes/ at S3/S4/S5 CHECK.
          This check covers client-side constant alignment only.

────────────────────────────────────────────────────────────────────────────────
CHECK 1 — CHAT INPUT
────────────────────────────────────────────────────────────────────────────────
DOM hook:     #chat-input
              worker-wb/public/index.html — confirmed present (line ~560)
              apps/operator-shell/src/ui/index.html — confirmed present
Attributes:   aria-label="Type a message"  ✓
              placeholder="Type a message…" (CSS ::placeholder via #chat-input)  ✓
              onkeydown → submit() fires on Enter without Shift  ✓
JS wiring:    Inline module in index.html imports sendMessage from ./session-view.js
              submit() trims value, disables controls, calls sendMessage()  ✓
              Re-enables and restores focus in finally block  ✓
Route:        SEND_ENDPOINT = '/ui/send' (session-view.js:29)  ✓
Disabled:     During in-flight send  ✓
Result:       PRESENT

────────────────────────────────────────────────────────────────────────────────
CHECK 2 — SEND BUTTON
────────────────────────────────────────────────────────────────────────────────
DOM hook:     #send-btn
              worker-wb/public/index.html — confirmed present (line ~566)
              apps/operator-shell/src/ui/index.html — confirmed present
Attributes:   aria-label="Send message"  ✓
              onclick → submit() in inline module  ✓
JS wiring:    Same submit() path as Enter key on #chat-input  ✓
Disabled:     During in-flight send; re-enabled in finally block  ✓
Result:       PRESENT

────────────────────────────────────────────────────────────────────────────────
CHECK 3 — TALK BUTTON (voice start)
────────────────────────────────────────────────────────────────────────────────
DOM hook:     #voice-btn-start  (.voice-btn.voice-btn-start)
              worker-wb/public/index.html — confirmed present (line ~577)
              apps/operator-shell/src/ui/index.html — confirmed present
Attributes:   aria-label="Start recording"  ✓
              .voice-btn-label text "Talk"  ✓
              Collapses to glyph only at ≤420px (.voice-btn-label display:none)  ✓
JS wiring:    Inline module imports startCapture from ./voice-capture.js  ✓
              startCapture() → getUserMedia({ audio: true })  ✓
              Triggers browser native mic permission prompt on first click  ✓
State badge:  #voice-status-badge aria-live="polite"  ✓
State machine:
  idle → requesting → recording   (granted)  ✓
  idle → requesting → denied      (denied)   ✓
  idle → requesting → denied      (no device) ✓
  idle → requesting → unsupported (no API)   ✓
Result:       PRESENT

────────────────────────────────────────────────────────────────────────────────
CHECK 4 — STOP/SEND AND RECORD CONTROLS
────────────────────────────────────────────────────────────────────────────────
DOM hook (stop):    #voice-btn-stop  (.voice-btn.voice-btn-stop)
                    worker-wb/public/index.html — confirmed present (line ~583)
                    apps/operator-shell/src/ui/index.html — confirmed present
Attributes:         aria-label="Stop and send recording"  ✓
                    .voice-btn-label text "Stop & Send"  ✓
                    Initially hidden (.hidden class)  ✓
JS wiring:          stopAndSend() from voice-capture.js  ✓
                    POST /voice/capture (VOICE_ENDPOINT)  ✓
                    multipart/form-data field "audio" filename "recording.webm"  ✓
                    State: recording → processing → sent → idle (3 s auto-reset)  ✓

DOM hook (cancel):  #voice-btn-cancel  (.voice-btn.voice-btn-cancel)
                    worker-wb/public/index.html — confirmed present (line ~589)
                    apps/operator-shell/src/ui/index.html — confirmed present
Attributes:         aria-label="Cancel recording"  ✓
                    .voice-btn-label text "Cancel"  ✓
                    Initially hidden (.hidden class)  ✓
JS wiring:          cancelCapture() from voice-capture.js  ✓
                    State: recording → idle (no upload)  ✓

Visibility:
  Stop & Send:  hidden when idle/requesting; shown only during recording  ✓
  Cancel:       hidden at idle; shown during recording and terminal states  ✓
Result:         PRESENT

────────────────────────────────────────────────────────────────────────────────
CHECK 5 — CALENDAR ENTRY (browser surface)
────────────────────────────────────────────────────────────────────────────────
Status bar:
  #status-calendar        — confirmed present (line ~461)  ✓
  #calendar-panel-toggle  — "⚙ Calendar" button confirmed (line ~462)  ✓

Drawer DOM hooks (all confirmed in index.html):
  #calendar-panel               ✓
  #calendar-panel-provider      ✓
  #calendar-panel-state         ✓
  #calendar-panel-reason        ✓
  #calendar-panel-detail        ✓
  #calendar-panel-guidance      ✓
  #calendar-panel-action-type   ✓
  #calendar-panel-run           ✓
  #calendar-panel-result        ✓
  #calendar-panel-result-state  ✓
  #calendar-panel-result-reply  ✓
  #calendar-panel-result-meta   ✓
  #calendar-panel-error         ✓

Action bar:
  #calendar-action-bar    ✓
  #calendar-action-result ✓

JS wiring:
  calendar-panel.js imported and wired  ✓
  refreshStatus() — GET /calendar/status on open and init  ✓
  triggerAction() — GET /calendar/status?run=1 on run click  ✓
  Six closed states implemented: unknown/ready/not_ready/unconfigured/
    unsupported/error  ✓
  #calendar-panel-run enabled for all states except unknown  ✓

Routes:
  GET /calendar/status       — CALENDAR_ENDPOINT in session-view.js:28  ✓
  GET /calendar/status?run=1 — appended by triggerAction()  ✓
Result:   PRESENT

────────────────────────────────────────────────────────────────────────────────
CHECK 6 — STATUS STRIP
────────────────────────────────────────────────────────────────────────────────
DOM hook:         .status-bar header — confirmed sticky top:0 z-index:20  ✓
Required hooks:
  #status-core          ✓  (line ~455)
  #status-state         ✓
  #status-session       ✓
  #status-calendar      ✓  (line ~461)
  #last-event-marker    ✓
JS wiring:
  session-view.js polls /session/latest every POLL_MS (5000 ms)  ✓
  #last-event-marker hidden at ≤768px via CSS  ✓
Result:   PRESENT

────────────────────────────────────────────────────────────────────────────────
CHECK 7 — MOBILE-SAFE LAYOUT
────────────────────────────────────────────────────────────────────────────────
Viewport meta:    width=device-width, initial-scale=1.0  ✓  (line ~5)
CSS breakpoints:
  ≤768px:  1-col 2-row panels grid; status-bar compact; voice-btn reduced  ✓
  ≤640px:  .calendar-panel width: 100vw  ✓
  ≤420px:  .voice-btn-label display:none  ✓
Chat bar: position:fixed, full width, above voice footer  ✓
Panel height: calc(100vh - header - chat-bar - voice-footer)  ✓
Result:   PRESENT

────────────────────────────────────────────────────────────────────────────────
SUMMARY
────────────────────────────────────────────────────────────────────────────────
Check                        Result    Notes
───────────────────────────  ────────  ──────────────────────────────────────
0  Explicit route wiring     PRESENT   Constants match declared route table;
                                       handler files verified at S3/S4/S5
1  Chat input                PRESENT   #chat-input → /ui/send via sendMessage
2  Send button               PRESENT   #send-btn → /ui/send via sendMessage
3  Talk (voice start)        PRESENT   #voice-btn-start → getUserMedia → /voice/capture
4  Stop & Send / Cancel      PRESENT   #voice-btn-stop / #voice-btn-cancel wired
5  Calendar entry            PRESENT   Full drawer + calendar-panel.js
6  Status strip              PRESENT   session-view.js polling every 5 s
7  Mobile-safe layout        PRESENT   Three CSS breakpoints confirmed

PRESENT count: 8 of 8
MISSING count: 0 of 8

S2-CHECK PASS CONDITIONS MET (repo state):
  ✓ Route wiring constants match declared handler files
  ✓ #chat-input and #send-btn present, wired to sendMessage(), posts to /ui/send
  ✓ Enter (no Shift) submits from chat input; focus restored after send
  ✓ Talk triggers getUserMedia; voice state machine wired in voice-capture.js
  ✓ Stop & Send posts to /voice/capture; Cancel returns to idle
  ✓ Calendar drawer present with all DOM hooks and calendar-panel.js wiring
  ✓ Status strip polls and updates all hooks
  ✓ Mobile breakpoints present for 375px / 640px / 768px viewports

DEPLOYED FILE:    worker-wb/public/index.html (Stage 4 operator shell)
DEPLOYED MODULES: session-view.js, voice-capture.js, calendar-panel.js
ASSET ROOT:       worker-wb/public/ (wrangler.jsonc assets.directory)

OPEN ITEMS (must close before CF-VERIFY-CHECK):
  - Confirm worker-wb/src/routes/ui-send.ts exists (S3)
  - Confirm worker-wb/src/routes/voice-capture.ts exists (S4)
  - Confirm worker-wb/src/routes/calendar-status.ts exists (S5)
  - Live browser test: Talk triggers mic permission prompt
  - Live browser test: Calendar drawer opens and run button calls /calendar/status?run=1
